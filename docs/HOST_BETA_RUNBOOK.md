

## §154 체인19 — §152 오디오 사인오프 실기 실증 3/3 · 무음 경계 기각 (2026-07-30)

§152 배선(e73cc42) 후 첫 실기 3연전. **세 회차 전부 F1 100**(각 1실행 — §126 규약대로
실행 수를 병기한다. 4/09 10/10 · 3/18 11/11 · 1/28 15/15, FP 0).

### 오디오가 실제로 경계를 만들었다 — 로그와 최종 산출물 대조

| 회차 | 사인오프 검출 | 오디오발 최종 경계 |
|---|---|---|
| 4/09 | 1건 → 후보 784.8·786.8 | **787.3**(라벨 787.1) |
| 3/18 | 3건 → 후보 188.0·190.0·577.8·579.8·726.3·728.3 | **190.3 · 580.3 · 729.3** — 최종 11경계 중 3개 |
| 1/28 | 1건 → 후보 357.8 | 회수 시각 360.0으로 합류 |

3/18의 726.3은 8차 실측에서 전량 STT 드리프트로 오해했던 그 신호다(실제 725.3~728.8,
라벨 729.0) — **창 한정 STT가 실기에서 정확한 시각을 냈다.** 오탐(라벨 없는 곳에
사인오프가 경계를 만든 사례)은 3회차 모두 0건.

### 무음 경계 감지는 실측 기각 (블라인드 라벨 28회차 · 355경계)

제품 `detectSilenceGaps`(피크 상대 임계)는 최선 설정에서 재현율 62%·오검출 분당 3.5개.
국소 대비(창 최저 RMS ÷ ±15s 중앙값)로 정규화를 바꾸면 재현율 100%가 되지만 **비경계
대조점 2032개의 82.2%도 같은 임계를 통과한다** — 문장 사이 무음이 경계 무음과 같은
깊이라 분포가 갈리지 않는다. 신호 자체의 부재이므로 파라미터로 구제 불가. 무음의 유효
용도는 스냅 보정(`snapCutPointToSilence`)에 한정. 실측 스크립트는 scratchpad
`silence-probe.mjs`·`silence-contrast.mjs`, 결론은 Plan(audio-anchor-cue) §2에도 기록.

### 방향 지시 (2026-07-30 사용자)

OpenAI 비전 개선 작업은 **보류**(현상 유지, 제거 아님). 분할 개선의 주 방향은 오디오
신호 확장. 다음 실측 대상은 §126 재현성 A/B(같은 회차 반복 실행에서 오디오발 경계가
흔들리지 않는지)와, 4형(4/07 220.8) 전용의 화자 분리(diarize) 판별력이다.

## §154-b 체인20 재현성 A/B — 3/18 4실행 전부 F1 100·최종 경계 동일 (2026-07-30)

3/18을 같은 빌드로 3연속 재실행(체인19 1실행 포함 총 4실행). **전부 F1 100이고 최종
경계 11개가 소수점까지 동일하다**(48.5 190.3 233.8 265.0 366.5 417.8 452.0 580.3
616.0 729.3 761.3). §126의 "같은 회차·같은 빌드가 실행마다 다른 결과"가 이 회차에서는
산출 수준에서 사라졌다.

내부 경로는 여전히 요동친다 — 그리고 그것이 비용을 좌우한다.

| 실행 | 회수 씨앗 | 유료 회수 프로브 |
|---|---|---|
| r1·r2 | 격자 0 · 띠 13 · **오디오 6** · 재심 3 | **22개** |
| r3 | 격자 128 · 띠 0 · 오디오 0 · 재심 1 | **129개** |

r3는 띠 이벤트가 0으로 나와(상류 §59 비결정성) 격자 전면 훑기로 후퇴했지만 같은 결과에
수렴했다. 사인오프 STT 시각 자체도 1.2초 안에서만 흔들렸고(726.7↔725.5) 최종 경계는
동일하게 729.3. 요약 — **오디오·띠의 표적 회수는 정확도의 필요조건이 아니라 (전면
훑기가 안전망으로 있는 한) 유료 프로브를 1/6로 줄이는 비용 절감 장치이며, 산출 결정성은
4실행 기준 성립.** 재현성 판정은 §126 규약대로 앞으로도 회차당 복수 실행으로 한다.

### 오프너 어휘 실측 — 고전 전환어 0/355, 제안자 기각 (같은 날)

경계 직후 창 355 vs 대조 355(로컬 Whisper base). "다음 소식/이어서/한편" 류 고전
오프너는 경계에서 **0회**(오히려 리포트 본문 어휘). 실존 반복 어휘의 정제 합집합 7종은
정밀도 98.2%(대조 오검출 1/355)나 비-첫 경계 커버 9.8%라 제안자 불가 — 회수 확인·가점용
보조 신호 후보로만 보류. **4형(4/07 220.8)은 어휘 단서 자체가 없다**(전환어·시점어 없이
새 주제 명사구 직행) — 어휘 경로로는 못 풀고 담화 구조 수준 판단이 필요. 상세는
Plan(audio-anchor-cue) §2와 scratchpad `opener-probe-report.md`.

## §143 체인13 4/09 — §141-c 기각, 202류 구조 확정, §139 제품화 (2026-07-29)

체인13 첫 회차 4/09가 85.7(FN 202.4 · FP 248·276.5)로 나왔다. 해부 결과 세 갈래다.

### ① FN 202.4 — 비전 판정이 옳은 구조적 계열

후보 208은 생성됐고 비전이 **4/4 만장일치로 배제**했다. 202.4는 앵커 없는 전면 분할
카드(§125 3형)라 "앵커샷 아님" 판정 자체가 옳다. 이전 실행들(95.2)의 생존은 §126 표 요동의
운이었다. 4/07 220.8(4형)과 함께 **비전 정의 밖 시작 유형**으로 묶인다 — 별도 메커니즘
없이는 못 잡고, 지금은 수단을 정하지 않는다(§112의 202류 데이터 보류와 동일 계열).

### ② §141-c(띠 프로브 960px) — 실측 기각

밀집 인용문은 540p에서도 한 덩어리(51~63행)로 읽혀 못 잡고(282+4·248+2 실측), 진짜 앵커
오판만 1건 늘었다(전수 355건: 270p 0건 → 540p 1건, 1/28 232.4). **득 0 실 1 — 원복.**
띠 기하(글리프·줄 수 공히)로 밀집 인용 FP는 원리적으로 못 가른다.

### ③ §139 위치 단서 — 회수 경로 전용으로 제품화

남은 판별자는 위치뿐이다. 관측 FP 5건(1/28 428·456, 4/09 254·282, 3/04 602) 전부
가운데·오른쪽 인물, 진짜 앵커는 전부 왼쪽(주말 포맷 포함 — 7/26 실측).

- `classifyAnchorShots(..., { anchorLeftDesk: true })`가 프롬프트에 위치 문장을 덧붙인다.
- **회수·되짚기(추가 경로)에서만 켠다.** 검증(배제) 경로는 그대로다 — 추가를 엄격하게
  하는 방향은 안전하지만, 배제를 엄격하게 하면 §92 오배제 0이 깨질 수 있다.
- 계약 테스트 2종: 기본 호출에 위치 문장 없음(§92 보호) · 회수 호출에 있음.
- 실증은 체인14(4/09 표적)에서. 그 전에는 "구현됨"까지만 말한다.

## §153 학습 게이트가 한 포맷의 붕괴를 가렸다 — 회차 회귀 게이트 추가 (2026-07-30)

블라인드 5회차를 코퍼스에 편입(106→111회차, 홀드아웃 18→21)하고 재학습 스윕을 돌렸다.
결과는 **합계 F1 83.5 → 84.1(+0.6)로 "개선"** 이었고 기존 게이트(`best.f1 > baseline.f1`)를
통과했다. 그런데 회차별로 뜯어보니 정반대였다.

| 회차 | 변화 |
|---|---|
| **6/28(일)** | 53.3 → **16.7 (−36.7)** |
| 7/23 | 55.6 → 52.6 (−3.0) |
| 2/19 | 82.4 → 88.9 (+6.5) |
| 4/07 | 83.3 → 92.3 (+9.0) |
| 5/20 | 85.7 → 95.7 (+10.0) |

**합계 F1은 TP/FP/FN을 풀링해 계산하므로 아이템이 많은 회차가 지배한다.** 주말처럼 표본이
적은 포맷은 통째로 희생돼도 합계에서는 +0.6로 보인다. 이 배치의 §134(측정이 실제를 안
비췄다)와 같은 계열의 결함이며, 대상이 실기 채점이 아니라 **학습 게이트**다.

시정 — `MAX_EPISODE_DROP = 5`점. 베이스라인 대비 회차 F1이 5점 넘게 떨어지면 합계가 올라도
반영을 막는다. `--write`로 검증: 6/28 −36.7이 잡혀 모델 파일이 변경되지 않았다.

**결론: 이번 후보는 반영하지 않는다.** 코퍼스 확장 자체(라벨 5회차)는 데이터이므로 유지하고,
모델 가중치는 커밋 상태를 지킨다 — 밤새 검증한 실기 18/19회차 100의 근거도 그 모델이다.

## §151 체인17·18 — §149·§150 실증, 성금 캠페인 사슬 종결 (2026-07-30)

| 회차 | 결과 | 실증 |
|---|---|---|
| 1/06 | 90.9→**100** | §149 — 회수 리드 794가 띠 필터 없이 생존 |
| 2/19 | **100** | 카나리아(§145→§149 이행 무해) |
| 1/14 | 94.7→**100** | **§150** — 716.4·730.2 짝 완성(체인15·16·17 3연속 FN 종결) |
| 1/28 | **100** | 카나리아(재스냅 변경 전역 무해 — 15경계) |

**성금 나눔 캠페인 FN 사슬(여섯 겹) 종결** — §142 프로브 사각 → §144 되짚기 −6 빗나감 →
§144-b 중복 필터 경계값 → §148-b 병합 문턱 → §148-a 병합 순서 → §150 재스냅 이음새.
네 회차(1/06·1/14·1/15·1/28)의 성금 구조가 전부 실기 100으로 마감됐다.

### 지표 ③ 회계 정정 (중요)

라벨 28회차 중 **9회차는 §127 때 코퍼스 학습셋에 등록된 in-sample**이라 ③에서 제외한다
(1/20·1/27·2/03·3/03·3/10·3/24·4/14·4/20·4/16). ③ 유효 블라인드는 **19회차** — 홀드아웃
3(1/13·2/10·4/07) + 미등록 16. 4/16은 사용 전 확인 규칙 위반이었으나 채점 전 적발(오염 0),
부산물로 독립 이중 판독이 코퍼스 정정 라벨과 12/12 일치(±8s — 라벨 품질 교차 검증).

**③ 현황: 19회차 중 18회차 최신 빌드 100 · 최솟값 96.3(4/07, §125 4형 구조).**
§126 변동 주의 — 이 표는 회차별 최신 실행 기준이며, "3연속 100"은 같은 빌드 복수 실행로만
판정한다(1/28은 §150 빌드 포함 근래 4실행 연속 100, 4/09·2/19·1/06도 최신 빌드 100).

## §149·§150 체인16 해부 — 띠 필터 전면 해제 + 재스냅 이음새 버그 (2026-07-30)

체인16: 1/15 **첫 100**(§148 실증) · 1/28 카나리아 100 · 1/14 94.7 · 1/06 90.9.

### §149 — 비전 ON에서 하단 띠 필터 전면 해제

1/06의 FN 794는 회수가 796을 찾았는데(회수 시각 로그 확인) **띠 필터가 지운 것**이다
(`배제 796.0`). §145(회수 추가분 한정)로도 부족했다 — 회수 추가분 자체가 진짜 리드일 수
있기 때문이다. 비전 경로에서 이 필터가 진짜 앵커를 지운 실측 누적 3건(687·560·796),
옳게 지우던 FP는 §139가 대신 잡는다(체인14 실증). **가치 0 위험 반복 → 전면 해제**
(비전 OFF 무료 경로는 유일한 방어라 유지).

### §150 — 재스냅 역방향 확장의 이음새 버그

1/14는 회수 720이 최종까지 살았는데 재스냅이 716.4로 못 당겨 720↔730.2가 10.2s로 붙어
§148 병합에 삼켜졌다. 정적 분석으로 원인 확정 — `refineBoundaryToTransition`의 역방향
확장에서 **확장 창의 마지막 프로브까지 상이하면**(전환이 확장 창과 첫 창의 이음새에 있을
때) `farIndex+1`이 없어 **원 경계를 반환**한다. 1/14가 정확히 이 기하다: 리드 716.4~721이
짧아 snap(720)의 첫 창 [716.5, 720.5]가 전부 앵커, 확장 창 [704.5, 716.25]가 전부 b-roll.
이음새(716.5)를 반환하도록 시정 + 단위 테스트.

검증 경로 FP 250(1/06, 기자회견 발언자 — 배제 1/4표)은 4실행 중 1회만 출현한 §126 요동
후보로 기록만 남긴다 — 검증 프롬프트에 위치 단서를 넣는 것은 §92 위험이라 회수 실증
없이는 하지 않는다.

## §148 성금 캠페인의 마지막 겹 — 최소 길이 병합 (2026-07-30)

체인15에서 §144-b가 작동하자(−2 프로브 24·36곳 진입) FN이 **리드에서 복귀 경계로
이동**했다 — 1/14 716.4→730.2, 1/15 822.3→839.2. 회수된 짝을 이번엔 **아이템 최소 길이
병합**이 삼켰다. 이 FN 하나에 겹이 다섯 개였다: 프로브 사각(§142) → 되짚기 오프셋(§144)
→ 필터 경계값(§144-b) → 병합 문턱(§148-b) → 병합 순서(§148-a).

- **§148-b** — 병합 문턱 15→12s. 성금 리드는 진짜 **13.8초** 아이템이다(라벨 28회차
  355아이템 중 최단). 1/06 15.0·1/28 15.1은 0.1초 차로 생존해 문턱 15의 문제를 가려 왔다.
  리드 조각(4~10s)은 12에서도 병합된다. STT 경로 기본값(15)은 유지.
- **§148-a** — 재스냅을 아이템 구성 앞으로. 회수 시각은 진짜 컷보다 최대 6초 늦어,
  구성-후-재스냅 순서에서는 진짜 16.9s 아이템이 pre-snap 13.5s로 읽혀 병합된다(1/15 실측).

3/18 카나리아 100 유지. 실증은 체인16(1/14·1/15 표적 + 1/06 15.0s·1/28 15.1s 경계 카나리아 —
문턱을 낮췄으니 경계 사례가 그대로 생존하는지 반드시 확인).

## §147 체인14 — §139·§145 실증 + §144의 경계값 버그 적발 (2026-07-29)

| 회차 | 이전 | 체인14 | 실증 |
|---|---|---|---|
| 4/09 | 85.7 | **100** | §139 — 대담 FP 248·282 소멸, 202.4(3형)도 이번 실행에선 TP |
| 3/18 | 95.7 | **100** | §139 — 토론 스튜디오 FP 138 소멸 |
| 2/19 | 97.0 | **100** | §145 — 검증 통과 후보를 띠 필터가 못 건드림 |
| 1/28 | 100 | **100** | 카나리아 유지 — §139가 진짜 회수를 해치지 않음 |
| 1/14 | 94.7 | 94.7 | §144 미작동 — 아래 |
| 1/15 | 96.0 | 96.0 | 〃 |

### §144-b — −2 프로브가 경계값에서 전멸했다

체인14 세 회차(1/14·1/15·2/19)의 되짚기 목록에 −2 후보가 **0개**였다. 원인은 한 줄:

- 띠 이벤트는 `plan.times`에 합류한다(§110 회수 구성, index.ts 2813행).
- 되짚기의 중복 판정 방지 필터는 `plan.times.every(|x − t| > 2)`.
- −2 후보는 자기 이벤트와 **정확히 2.0초** 거리 → `2 > 2 = false` → 전부 제거.
- −6 후보는 6초 거리라 통과 — 그래서 목록이 구판과 똑같아 보였다.

`> 2`를 `> 1.5`로 — 2초 스캔 격자에서 2초 떨어진 프레임은 다른 프레임이고, 재심 오프셋
(+1.2)과의 우발 중복만 걸러진다. 교훈: **오프셋 상수를 바꿀 때는 그 값이 지나갈 모든
필터의 경계값을 함께 검산할 것.** 실증은 체인15(1/14·1/15 표적 + 3/18 카나리아).

## §146 체인13 전량 + §145 띠 필터를 회수 한정으로 (2026-07-29)

체인13(§141-b·§142 빌드, 7회차) 결과.

| 회차 | F1 | 판정 |
|---|---|---|
| 2/11 | **100** (96.3→) | **§141-b 실증** — 작은 폰트 회차의 오배제 해소 |
| 4/23 | **100** | 신규 블라인드 첫 실행 |
| 2/19 | 97.0 | FN 559.6 — §145 표적(아래) |
| 3/18 | 95.7 | FP 136.8 — 토론 스튜디오 가운데 인물, §139 표적 |
| 1/15 | 96.0 | FN 822.3 — §142가 아니라 §144 계열(아래) |
| 1/14 | 94.7 | FN 716.4 — §144 표적(빌드에 미포함이었음) |
| 4/09 | 85.7 | FN 202.4(§125 3형 구조) · FP 248/276.5(§139 표적) |

### 1/15 822.3의 정정 — §142 수혜자가 아니었다

격자 균등 훑기는 **경고 회차(bankFit>0.1) 전용**이다(§107-b). 1/15는 비경고라 격자 0장 —
§142(뒤 완충)는 이 회차에 아예 관여하지 않는다. 실제 경로는 띠 이벤트(828) → 되짚기
−6=**822.0** → 진짜 시작 822.3보다 0.3초 앞이라 직전 리포트 b-roll → 비앵커. **§144(−2
프로브)의 두 번째 수혜자다.** §142의 실수혜는 1/14뿐이며 그 1/14도 §144가 진짜 처방이다
(692→730 구간 38s<100라 격자 훑기 자체가 없다). §142는 경고 회차의 사각을 좁히는
보조 수정으로 자리매김한다.

### §145 — 띠 필터는 회수 추가분에만

2/19 560(구례 오리 AI 앵커)은 비전 검증을 통과(배제 2/4표)한 **본선 후보**인데 띠 필터가
지웠다. 원인 프레임은 데스크 잡동사니가 띠 위 밝은 구간으로 흡수돼 "가짜 첫 줄"(10행,
23~34% dark)을 만든 경우로, 글리프 규칙(10<12)·줄수 규칙(2줄<20) **어느 기하로도 오판**한다
— 구 규칙이었어도 지웠다(§141-b 회귀 아님).

구조적 처방 — 비전 ON에서 띠 필터는 **회수·되짚기가 추가한 경계에만** 적용한다. 비전
4프로브 검증을 통과한 후보를 무료 luma 휴리스틱이 뒤집는 것은 위계가 거꾸로다. 비전
OFF에서는 검증이 없으므로 기존대로 전 후보를 검사한다(무료 경로 방어 유지).

## §144 되짚기 −6 한 점이 0.4초로 빗나갔다 — −2 프로브 추가 (2026-07-29)

체인13 1/14가 §142 빌드에서도 FN 716.4 그대로였다. 해부 결과 **모든 방어선이 한 끗씩 빗나갔다.**

- 격자 프로브: 692→730 구간이 38s(<maxSpan 100)라 훑지 않음 — §142와 무관한 구간.
- 띠 이벤트: 722(성금 카드 출현)에 발화 → 프레임은 카드 → 비전이 옳게 비앵커.
- §123 되짚기: 722−6 = **716.0**을 프로브 — 진짜 시작 716.4보다 **0.4초 앞**이라 직전
  리포트의 b-roll이 잡혔고 비전이 옳게 비앵커. 리드가 4.6초(716.4~721)뿐이라 −6이 컷을
  지나쳐 버린다(§123의 6초는 3/24 실측에서 나온 값 — 리드 7초 이상을 가정).

시정 — 되짚기가 이벤트 −6과 **−2** 두 점을 본다(720은 앵커 실측). 비용은 실패 띠 지점당
+1장. −2가 이벤트에 가까워도 안전한 이유: 이벤트가 리포트 중간 띠 교체라면 −2도 리포트
화면이라 비앵커로 자연 소거되고, 진짜 짧은 리드라면 −2가 리드 안에 떨어진다. 채택은
기존 §123 중간점 연속성·§138 병합을 그대로 거친다.

교훈 — 성금 캠페인 리드는 회차마다 4~17초로 흔들린다(1/14 4.6s · 1/28 15s · 1/15 17s).
고정 오프셋 하나로는 이 분포를 못 덮는다. 두 점(−6·−2)이면 4~13초 리드를 모두 문다.

## §142 회수 프로브 뒤 완충 20s → 8s — 발견 기회조차 없던 FN (2026-07-29)

체인12에서 §138이 1/28 702·1/06 809를 회수했는데 **1/14 716.4만 그대로 남았다.** 로그를
보면 §138이 실패한 게 아니라 — 구간 592→732의 프로브가 `tailEdgeSeconds = 20` 탓에 712에서
멈춰 **716.4는 비전에 보여진 적조차 없다.**

20초 완충의 실질 역할은 "다음 앵커 리드를 잘못 물어 중복 경계를 만드는 것" 방지였는데,
그 방지는 이제 §138 중간점 병합이 맡는다 — 다음 경계와 8초 안이면 같은 경계로 접히고,
8~20초면 중간점이 같은 샷일 때(리드 연장부) 폐기된다. 완충을 F1 허용오차와 같은 8초로
줄인다. 오프라인 시뮬레이션(22회차): 커버되는 FN +1(1/14 716), 프로브 +15%(경고 회차 한정).

체인12 전량 결과(§138 실증, 회귀 0) — 예측이 실제 산출물 로그(§134) 기준이다.

| 회차 | F1 | 판정 |
|---|---|---|
| 1/28 | **100** (82.8→) | §135r+§136+§138 합작 — FP 0 · FN 0 |
| 1/06 | **100** (95.2→) | §138이 809.0 회수 |
| 3/12 | **100** | 카나리아 — §138이 멀쩡한 회차를 깨지 않음 |
| 1/14 | 94.7 | FN 716.4 — §142 대상(프로브가 712에서 끊김) |
| 2/11 | 96.3 | FN 1004.4 — §141-b 대상(작은 폰트 오배제) |
| 1/15 | 96.0 | FN 822.3 — **§142의 세 번째 수혜자** |

1/15 822.3은 §142 진단을 독립 재확인한다 — 689.8→839.2 구간(149s)에서 tailEdge 20이
프로브를 819에서 끊어 성금 캠페인 리드(822.3~839)가 비전에 보인 적이 없다. §142(8s)면
823.8·827.8 프로브가 리드에 떨어지고, §138 중간점(≈831 = 성금 카드)이 별개 아이템으로 가른다.

### §141-b — 줄 수를 주판별자로 승격, 절대 글리프 규칙 대체

2/11 1004.4의 원인은 §141 초판으로도 안 잡혔다 — 구 글리프 규칙(<12행=인용)이 남아 있었고,
**2/11은 헤드라인 폰트가 계통적으로 작아**(10~14행) 진짜 앵커가 그 규칙에 걸린다(1004 후보
1008의 +2·+4 프로브 모두 인용 판정 — 오프라인 선확인). 회차 간 폰트 크기 변동에 절대
임계는 원리적으로 못 버틴다. 줄 수는 회차가 바뀌어도 불변이다.

최종 규칙 — `textLines >= 2 && maxGlyph < 20행(270p)`만 남긴다(글리프<12 삭제).
전수 검증(라벨 26회차·진짜 앵커 338건·비첫아이템): 구 규칙 오판 3건 → **§141-b 0건**.

## §141 띠 텍스트 줄 수 규칙 — 사용자 제공 도메인 지식 (2026-07-29)

사용자가 결정적 단서를 줬다 — **앵커 헤드라인 띠는 텍스트 1줄, 인터뷰샷 인용 띠는 2줄.**

실측(270p 어두운 행 런 분해)이 이를 확인했다.

| 유형 | 사례 | 런 |
|---|---|---|
| FP(발언·대담) | 1/28 456 · 4/09 254 · 4/09 282 · 7/26 780 | 11+11 · 12+11 · 17+11 · 17+12+11 |
| 진짜 앵커 | 1/28 163 · 3/04 684 · 1/15 349 · 7/26 700 | 22 · 26 · 25 · 25 (전부 1줄) |

인용 자막은 줄당 11~17행이라 **글리프 높이 규칙(≥12=헤드라인)을 비켜가지만**, 줄 수로는
정확히 갈린다. §140에서 "띠 기하로는 원리적으로 불가"라 했던 판정은 **글리프 높이에 한한
얘기였다** — 줄 수는 별개의 축이고, 이 축은 판별력이 있다.

규칙 — `textLines >= 2 && maxGlyph < 20행(270p)`이면 인용띠. 20행 상한이 앵커샷의 인서트
영상 잡음(1/28 580: 12+**25** — 25행 런이 진짜 헤드라인)을 보호한다. 판별 마진: 인용 줄
최대 17 < 20 < 진짜 헤드라인 최소 22.

검증 — 라벨된 진짜 앵커 시작 **315건 전수**(첫 아이템 면제 반영)에서 새 규칙의 신규 오판
**0건**. 전수에서 나온 양쪽-프로브 인용 판정 2건(2/25 602.7 · 2/11 781.6)은 글리프 2~10의
**구 규칙 소행**으로 이번 변경과 무관하며(줄 수 0~1), 실기에서는 생존해 온 기존 한계다.

남는 것 — 1/28 428 · 3/04 602는 발언 샷인데 자막이 **헤드라인형 1줄 요약**이라 띠로는
원리적으로 못 가른다. 이 둘이 §139(위치 단서)의 최종 대상이다.

## §140 체인11 — §135·§136·§137 실기 실증 (2026-07-29)

| 회차 | 이전 | 이번 | 무엇이 바뀌었나 |
|---|---|---|---|
| 1/28 | 82.8 | **90.3** | 579.5 재스냅 복구(§136) · 687 오배제 해소(§135) |
| 3/04 | 95.2 | **100** | 회수 FP 602(0.92) 소멸(§137) |
| 4/09 | 95.2 | 95.2 | 되짚기 FP 276.5(0.98) — 임계로는 못 잡음(예상대로) |
| 3/12 | — | **100** | 신규 블라인드 첫 실행 |

**회귀 0건.** 그리고 §134 로그가 목적대로 작동해, 채점이 후보·회수 값 재구성이 아니라
`최종 아이템 시작:` 한 줄로 끝났다(`scratchpad/score-run.mjs`).

### §137의 한계가 실기로 드러났다

1/28 428의 비전 신뢰도가 지난 실행 **0.91 → 이번 0.96**으로 올라 임계를 통과했다.
**신뢰도는 이 계열의 판별자가 아니다** — 같은 프레임이 실행마다 다른 확신을 받는다(§126).

### 띠 기하도 이 계열을 못 가른다 (오프라인 실측)

`scratchpad/band-offline.mjs`로 실기 없이 임의 프레임을 띠 필터에 걸어 보았다.

| 프레임 | 글리프 | 판정 |
|---|---|---|
| 4/09 282 (FP) | 17 · 31 | 헤드라인 |
| 1/28 428 (FP) | 22 · 22 | 헤드라인 |
| 1/28 580 (진짜 앵커) | 25 · 25 | 헤드라인 |

인터뷰 인용 자막의 글자가 헤드라인만큼 크다. **띠 기하로는 원리적으로 불가**다 — 남은
지렛대는 §139(위치)뿐이다.

### §139의 전제 하나가 확인됐다

주말 포맷 앵커도 왼쪽이다(7/26 700s 실측 — 남성 앵커 왼쪽 데스크, 오른쪽 복숭아 영상).
다만 프롬프트 변경은 **회수 경로에만** 적용해 §92 오배제 0을 건드리지 않는 설계로 간다.

## §138 회수 병합의 20초 일괄 폐기 — FN 5건 중 3건이 이 한 줄이었다 (2026-07-29)

```ts
if (merged.every((existing) => Math.abs(existing - time) > 20)) merged.push(time);
```

회수가 찾은 경계가 기존 경계와 20초 안이면 무조건 버렸다. 훑기 격자가 거칠어 같은 앵커
블록에서 여러 번 발견되는 것을 막으려던 규칙인데, **KBC의 성금 나눔 캠페인 구조를 통째로
삼켰다** — 짧은 앵커 리드 → 전면 성금 카드 → 앵커 복귀가 13~16초 간격이라 두 번째 앵커가
언제나 폐기된다. 세 회차에서 같은 자리가 FN이었다(1/28 702.1 · 1/06 809.0 · 1/14 716.4).

이 구조는 예외가 아니라 **정규 편성**이다. 1/06·1/14·1/15·1/28 네 회차에서 모두 "리드 라벨 +
카드 뒤 복귀 라벨"이 쌍으로 존재한다.

시정 — 8초(F1 허용오차) 안은 같은 경계로 보고, 20초 밖은 그대로 채택하며, **8~20초 사이는
중간점이 같은 샷인지로 가른다**(§123과 같은 착상). 같은 블록의 중복 발견이면 중간점도 그
앵커샷이고, 진짜 다음 아이템이면 중간에 카드·현장이 들어간다.

| | 중간점 거리 |
|---|---|
| 같은 블록 중복(버려야 함) | 0.002 · 0.003 · 0.003 · 0.050 |
| 진짜 다음 아이템(살려야 함) | 0.270 · 0.319 · 0.339 · 0.367 |

마진 5배. 문턱은 §135와 같은 0.15를 쓴다. 못 재면 예전 동작(폐기)을 따른다 — 회수는 추가라
못 재는 쪽에서 경계를 늘리지 않는다.

### §135 정밀화 — 같은 샷인 프로브의 표만 센다

두 프로브 모두 같은 샷일 것을 요구했더니 필터가 너무 무뎌졌다. 1/28 428(발언 샷)은 +2s는
같은 샷인데 +4s가 샷을 벗어나(0.055 vs 0.345) 필터가 통째로 비켜갔고 FP가 살아남았다.
**같은 샷인 프로브의 표만 세고, 표가 하나도 없으면 배제하지 않는다**로 고친다. 687은 두
프로브 모두 다른 샷이라 표가 0 → 여전히 보호된다.

## §139 (미착수 후보) 고신뢰 발언·대담 FP — 임계로는 못 잡는다

1/28 428은 실행마다 0.91 ↔ 0.96으로 흔들려 §137 임계(0.95)를 그때그때 통과한다. 신뢰도는
이 계열의 판별자가 아니다.

프레임 증거가 가리키는 판별자는 **화면 내 위치**다 — 관측된 FP 5건(1/28 428·456 오른쪽
인터뷰이, 4/09 254·282 가운데 대담 참석자, 3/04 602 가운데 회견 발언자)은 전부 왼쪽 데스크가
아니고, 진짜 앵커는 전부 왼쪽이다.

**다만 프롬프트의 기존 규칙("이름·직함 띠면 false")이 원리적으로 안 통하는 반례를 찾았다** —
7/26(일) 200s의 대담 게스트는 가운데에 앉아 **헤드라인 띠**를 달고 있다. 위치 단서를 넣는
쪽이 맞아 보이나, 주말·특집 포맷의 앵커가 항상 왼쪽인지 확인하기 전에는 넣지 않는다
(프롬프트 변경은 전 회차에 영향을 주는 가장 위험한 변경이다 — §92·§101-b·§113-a 모두 실기
검증을 거쳐 넣었다).

## §134 측정 위생 — 블라인드 채점이 실제 산출물이 아니었다 (2026-07-29)

**이 절이 이 배치에서 가장 중요하다. 결함 진단보다 먼저 읽을 것.**

파이프라인은 최종 아이템 **개수만** 로그에 남기고 경계 시각은 어디에도 기록하지 않았다.
그래서 사후 채점이 후보·회수 로그에서 값을 재구성했는데, 그 값은 **하단 띠 필터·재스냅
이전**이라 실제 산출물과 다르다.

| 회차 | 재구성으로 보고한 값 | 실제(시퀀스 인점) |
|---|---|---|
| 4/09 | 90.9 | **95.2** |
| 1/28 | 83.9 | **82.8** |
| 3/04 | 95.2 | 95.2(일치) |

### 실제 결과를 읽는 방법

아이템 시퀀스는 원본을 복제해 **시퀀스 인/아웃점**으로 구간을 잡는다(`setSequenceRange`).
따라서 트랙 아이템의 `getInPoint()`는 **언제나 0**이다 — 그것으로 읽으면 전부 0.0이 나온다.
`sequence.getInPoint()`를 읽어야 한다. 회차 식별은 `sequence.getEndTime()`(소스 전체 길이)이
유일한 지문이다. 이름(`YYYYMMDD_news_NN`)의 날짜는 **생성 날짜**이지 회차 날짜가 아니다.

읽을 때 걸린 함정 셋(전부 실제로 틀린 표를 만들었다).

1. 인덱스만으로 정렬하면 생성 날짜가 다른 시퀀스가 뒤섞인다(`20260727_news_280` ↔ `20260728_news_280`).
2. 같은 회차를 연속 두 번 돌리면 인덱스가 이어져 두 실행이 한 블록으로 합쳐진다 —
   한 실행 안에서 시작 시각은 단조 증가하므로 **감소하면 새 실행**으로 가른다.
3. 길이 지문 허용치를 1.5초로 두면 블라인드 폴더에 없는 소스의 시퀀스가 길이가 비슷한
   회차로 잘못 붙어 남의 결과를 그 회차 점수로 만든다. **0.2초**로 조일 것.

### 조치

- 최종 아이템 시작 시각을 `activity` 로그에 기록.
- 하단 띠 필터가 배제한 **시각**을 개수와 함께 기록 — 개수만으로는 이 필터가 진짜 앵커를
  지워도 사후 판독이 불가능하다(§135가 바로 그 사례다).

### 재채점 결과 (실제 시퀀스 인점, 블라인드 22회차)

**100인 회차 15/22 · 최솟값 82.8(1/28).** 그리고 §126 변동이 같은 빌드에서 재확인됐다 —
2/10 76.9↔100, 3/10 97.1↔100, 1/21 96.3↔100.

## §135 하단 띠 필터가 진짜 앵커를 지웠다 (2026-07-29)

1/28의 687(성금 캠페인 출범 앵커샷)이 필터에 지워졌다. §92 "오배제 0" 원칙이 **무료 필터**
쪽에서 깨진 첫 사례다.

기전 — 필터는 후보의 `+2s`·`+4s`를 본다. **앵커 리드가 4초보다 짧으면 두 프로브가 다음
꼭지에 떨어진다.** 687의 리드는 약 4초였고, 692·694는 이미 성금 카드 화면이었다. 카드
하단의 계좌번호 잔글씨가 "흰 띠 + 큰 글자 없음" 조건에 걸려 인용·이름표 띠로 판정됐다.

시정 — 프로브가 후보와 **같은 샷일 때만** 그 판정을 채택한다(`isSameShotGrid`, 휘도 격자
평균 절대차 0.15 미만). 문턱은 실측으로 잡았다.

| 사례 | +2s | +4s |
|---|---|---|
| 진짜 앵커 4건 | 0.002~0.051 | 0.002~0.060 |
| 필터가 **옳게** 지운 발언·대담 2건 | 0.013·0.028 | 0.018·0.032 |
| 오배제 원인 1/28 690 | **0.384** | **0.371** |

격자를 못 얻으면 `false`(배제 보류)를 준다 — 못 재는 쪽이 진짜 앵커를 지키는 방향이다.

## §136 격자 확보 실패를 영구 캐시했다 — 재스냅 소실의 진짜 원인 (2026-07-29)

1/28의 588이 라벨 579.5까지 당겨지지 않아 FP·FN을 동시에 만들었다(Δ8.5s, 허용 8초를 0.5초 초과).

처음에는 "오른쪽 인서트 영상이 움직여 유사도 판정이 막혔다"고 의심했으나 **실측이 이를
기각했다** — 579.5~588.5의 격자 거리는 0.002~0.004로 이미 "실질 동일"이고, 579.0에서
0.298로 튄다. 즉 전환 지점은 깨끗하고 재스냅은 이를 찾았어야 한다.

진짜 원인은 `grabGrid`이 **null을 캐시**한 것이다. 프레임 추출이 한 번 간헐 실패하면 그
시각은 영영 오염되고, `refineBoundaryToTransition`은 격자 없음을 "정착과 상이"로 읽어
경계를 되돌리지 못한다(정착 프레임 자체가 실패하면 즉시 원 경계를 반환한다).

**§121·§122·§132에 이은 네 번째 "판정 프레임 확보 실패" 계열이다.** 이 프로젝트에서 지금까지
고친 결함의 과반이 모델·프롬프트가 아니라 프레임 확보였다는 사실을 기억할 것.

시정 — 1회 재시도 · 실패 미캐시 · 실패 시각 경고 로그.

## §137 회수 승격 임계 0.9 → 0.95 (2026-07-29)

회수는 검증과 방향이 반대다 — 검증은 배제(오배제가 위험)라 0.85로 느슨하지만, 회수는
추가라 오검출이 곧 과분할이다.

전 실행 로그의 앵커 판정을 라벨과 대조한 결과, **0.95 미만은 2건뿐이고 둘 다 FP**였다.

| 신뢰도 | 참 | 거짓 |
|---|---|---|
| 0.90~0.92 | 0 | 1 (1/28 428 인터뷰이) |
| 0.92~0.95 | 0 | 1 (3/04 602 회견 발언자) |
| 0.95~0.98 | 1 | 0 |
| 0.98~1.00 | 58 | 24 |

0.9→0.95는 잃는 참 회수 없이 FP만 줄인다. 다만 **0.98 이상의 FP 24건은 임계로 못 잡는다** —
4/09 254(대담 참석자 0.98)·282(되짚기 0.98)가 그 예다. 이 계열은 별도 접근이 필요하다.

## §133 §132 검증 + 30회차 누적 (2026-07-29)

### §132 프레임 부족분 재수집 — 목표대로 작동

1/21 재실행에서 문제의 후보가 정확히 해소됐다.

| | 수정 전 | 수정 후 |
|---|---|---|
| 후보 192 확보 | **2/2** (표 상한 2 < 문턱 4 → 배제 불가) | **3/3** → 배제됨 |
| 1/21 F1 | 96.3 (FP 1) | **100** (13/13) |

### 같은 배치 3회차도 전원 100

| 회차 | F1 | 특기 |
|---|---|---|
| 2/11 | **100** (14/14) | 후보 10개 중 **7개 배제**, 회수 11건 |
| 4/02 | **100** (14/14) | 배제 4 · 회수 6 |
| 2/25 | **100** (13/13) | 하이라이트 예고 없이 로고→앵커 디졸브(8.1s) |

**2/11이 §124의 가치를 직접 증명했다.** 후보 10개 중 7개를 배제해 잔여가 3개였다 — 옛 문턱(잔여 3 미만이면 해제)의 정확히 경계선이다. 그 7건은 전부 옳은 배제였고 회수가 11건을 채웠다. 후보 하나만 적었어도 옛 규칙에서는 7건이 통째로 되살아나 2/10(F1 76.9)과 같은 붕괴가 났을 것이다.

2/25는 **하이라이트 예고가 없는 회차**다(로고에서 바로 앵커로 디졸브, 첫 아이템 8.1s). 오프닝 구조에도 회차 편차가 있다는 실측이다.

### 누적 — 블라인드 30회차 중 27회차 F1 100

남은 결손 3건은 전부 성격이 확정돼 있다.

| 회차 | 결손 | 성격 |
|---|---|---|
| 4/07 | FN 220.8 | 앵커샷 없는 시작(§125) — 원리적 한계 |
| 1/06 | FN 809.0 | 같은 유형 |
| 1/14 | FN 716.4 | 캠페인 리드 4.2초(§130·§131) — 사용자가 "분할 불필요"로 확인한 꼭지라 실용상 무해 |

**현재 구조에서 고칠 여지가 있는 결손은 §132로 소진됐다.**

### 이번 세션 수정 6건의 공통점

| § | 수정 | 성격 |
|---|---|---|
| §121 | 회수 경로 내보내기 재시도 | 프레임 확보 |
| §122 | 검증 프레임 워밍업 | 프레임 확보 |
| §123 | 되짚기 중간점 연속성 | 판정 규칙 |
| §124 | 안전망 문턱 3→1 | 판정 규칙 |
| §132 | 프레임 부족분 재수집 | 프레임 확보 |
| §124-d 외 | 계측 4종 | 관측성 |

**여섯 중 셋이 "판정에 필요한 프레임을 제대로 못 모은" 문제였다.** 모델도 프롬프트도 아니다. 그리고 그 셋을 찾아낸 것은 전부 관측성 보강이었다 — 계측 없이는 표 수만 보이고 분모가 안 보인다.
