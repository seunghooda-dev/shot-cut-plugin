# Plan — 긴 아이템 재탐색으로 미검출 회수 (long-item-rescue)

> §100 처방. 신규 회차에서 무너진 재현율을 **이상 구간에서만** 회수한다. 정상 회차는 건드리지 않는다.

## 1. 문제

미학습 신규 회차 2026-07-23에서 무료 분할이 무너졌다 — 정답 12아이템 중 **4개**만 생성(F1 47.1, 실기 비전 ON 50.0). 직전 회차 7/20·7/21·7/22는 F1 100이다.

§100 실측 요약.

- 참조 뱅크(평일 13장)가 이 회차 앵커샷을 못 알아본다 — 최소거리 0.0784(7/22) → **0.1179**(7/23).
- 뱅크 거리가 **학습 모델의 특징이기도 해서** 후보 생성과 모델이 **같은 원인으로 함께** 무너진다(모델 시작 12→0). 이중화처럼 보였지만 이중화가 아니다.
- 매처는 "왼쪽에 앵커가 있는가"가 아니라 **"화면 전체가 참조와 닮았는가"**를 잰다 — 가중치 `1/(√std+8)` 아래에서 **배경이 총 가중치의 68%**를 차지한다. 놓친 8샷과 잡은 4샷이 육안으로 구분되지 않는 이유다.

## 2. 기각된 대안 (실험 완료 — 다시 시도하지 말 것)

| 시도 | 결과 | 판정 |
|---|---|---|
| 모델에서 뱅크 거리 특징 제거 후 재학습 | 홀드아웃 최고 77.8 < 기준선 82.6, 7/23도 47.1→44.4 | **기각** — 현재 특징 공간에 뱅크 외 판별 정보가 없다 |
| 앵커 영역만 매칭(열6 행0~8) + 재학습 | 7/23 47.1→75.0이지만 전량 94.4→**91.2**, 망가진 회차 4→**11** | **기각** — 1회차 살리고 7회차 깬다 |
| 전체화면 후보 ∪ 영역 후보 | 7/23 **37.5**로 악화 | **기각** — 거리 척도가 달라 적응 임계가 깨진다 |
| (2026-07-21 선행) 왼쪽 영역 필터 | 모든 임계에서 하락 — 왼쪽 열에 앵커 **의상**이 들어가 매일 바뀐다 | **기각**([anchor-left-feature](anchor-left-feature.plan.md)) |
| (2026-07-21 선행) 시간적 움직임 신호 | TP/FP 중앙값이 겹침 — 스튜디오 배경이 애니메이션 비디오월 | **기각**([anchor-motion-signal](anchor-motion-signal.plan.md)) |

**교훈**: 고정 참조 13장 + 전역 가중치 하나로는 "배경 강건성"과 "앵커 특이성"을 동시에 만족시킬 수 없다. 매처 자체를 손보는 길은 네 번 막혔다.

## 3. 채택 방향 — 이상 구간 재탐색

**핵심 관찰**: 진짜 앵커들은 후보 목록에서 **사라진 게 아니라 임계 아래로 밀려 있다.** 7/23 정답 12개의 거리 순위는 상위 100 안에 8개, 150 안에 11개다(7/22는 12개 전부 상위 100 안).

그러나 전 구간에서 임계를 낮추면 후보가 150개로 늘어 비전 비용이 9배가 된다. 대신 **아이템 길이 이상**을 신호로 삼아 그 구간에서만 다시 찾는다.

### 알고리즘 (오프라인 검증 완료)

1. 확정된 아이템 중 길이가 `MAX_SPAN`(잠정 180s)을 넘는 것을 고른다.
2. 그 구간 안쪽(양 끝 `MIN_SPAN` 여유 제외)에서 **뱅크 거리가 가장 낮은 지점 하나**를 뽑아 후보로 추가한다.
3. 쪼개진 두 구간이 여전히 `MAX_SPAN`을 넘으면 반복한다.
4. 추가된 후보는 **기존 비전 검증 경로를 그대로 통과**시킨다(오배제 0 확립 — §92).

### 파라미터 스윕 결과 — 천장 63.2에서 막힘 (2026-07-27)

maxSpan(120~210) × minSpan(15~30) × 구간당 채택수(1~2) 격자 탐색 + 컷 크기 제안 변형까지 전부 돌렸다. 지표는 `raw`(비전이 아무것도 못 걸러낸 최악)와 `oracle`(비전이 가짜를 전부 걸러낸 최선)을 함께 냈다.

| 제안 신호 | 설정 | 전량 raw | 전량 oracle | 7/23 raw | **7/23 oracle** |
|---|---|---|---|---|---|
| 기준선(재탐색 없음) | — | **96.9** | — | 47.1 | — |
| 뱅크 거리 | max210 min25 per1 | 96.5 | 97.0 | 57.1 | **63.2** |
| 뱅크 거리 | max120 min25 per1(후보 268개) | 85.2 | 97.1 | 48.0 | **63.2** |
| 컷 크기 | max210 per12 | 94.1 | 97.1 | 41.4 | **63.2** |
| 컷 크기 | max150 per12 | 88.4 | 97.1 | 36.4 | **63.2** |

**어떤 신호·어떤 파라미터를 써도 7/23 oracle이 63.2에서 멈춘다.** 후보를 268개까지 늘려도 진짜는 3개뿐이다.

### 왜 막히는가 — 무료 신호의 한계 (실측)

7/23 뱅크 거리 분포는 최소 0.1179 · 10%p 0.1346 · **중앙 0.1803**인데, 놓친 경계들의 최근접 거리가 이렇다.

| 정답 경계 | 뱅크 거리 | 위치 |
|---|---|---|
| 758.38s | **0.1991** | **중앙값보다 나쁨** |
| 497.91s | 0.1610 | 10%p 밖 |
| 624.54s | 0.1591 | 10%p 밖 |
| 799.63s | 0.1529 | 10%p 밖 |

뱅크는 이 앵커샷들을 **무작위 프레임보다도 덜 앵커답다고 본다.** 임계·영역·구간 조정으로는 원리적으로 못 찾는다.

컷 신호는 뱅크와 무관해 유망해 보였다 — 놓친 8개의 **전체** 컷 순위가 37·42·45·63·59·11·125·120으로 전부 상위 125위 안이다. 그러나 480초짜리 footage 구간 안에는 b-roll 컷이 수십 개라 **구간 안에서는** 진짜 앵커 컷이 묻힌다. 전체 순위가 높아도 국소 경쟁에서 진다.

### 남는 효과와 비용

| | 값 |
|---|---|
| 채택 가능 최선(뱅크 거리 max210 min25 per1) | 7/23 47.1 → raw **57.1** · oracle 63.2 |
| 전량 대가 | 96.9 → 96.5 (raw), 97.0 (oracle) |
| 추가 후보 | 13개 · 7회차만 발동(비전 52장) |

### 회귀 위험이 구조적으로 낮다

코퍼스 정답 아이템 **937개 중 180s 이상은 8개(0.9%)**, 8개 회차에만 존재한다(3/06 236s, 6/03 328s, 6/29 288s 등). 즉 **99.1%의 아이템은 이 경로를 아예 지나지 않는다.** 진짜 긴 아이템에는 후보 1~2개가 더 붙지만 비전이 걸러낸다 — 그리고 비전 OFF 사용자를 위해 **비전이 켜져 있을 때만** 발동시키는 안을 우선 검토한다(무료 경로 정밀도 보호).

## 4. 목표 (스윕 후 하향 조정)

- **주 목표**: 7/23 무료 경로 F1 **47.1 → 57**(oracle 63.2), 코퍼스 전량 **96.9 → 96.5 이내**. 당초 목표였던 70+는 **무료 신호로 도달 불가능함이 실측으로 확인됐다**.
- **부 목표**: 발동 회차를 코퍼스의 10% 이내로 제한(현재 7/82).
- **비목표**: 매처·모델 변경(5종 전부 기각), 모닝와이드 지원.

**판단이 필요한 지점**: 47.1 → 57(비전 포함 시 63)이 **구현 가치가 있는가**. 전량 0.4 손실과 신규 회차 10~16점 회복의 교환이다. 안전망(§100 처방 ③)이 이미 "이 회차는 못 믿는다"를 알려주므로, 회수 자체를 포기하고 사용자 수동 보정에 맡기는 선택지도 유효하다.

## 5. 미해결 질문 (Design 단계에서 결정)

- `MAX_SPAN` 값 — 180s는 7/23 한 회차로 고른 값이다. 코퍼스 전량 스윕으로 확정할 것.
- **비전 OFF일 때 발동시킬 것인가.** 발동하면 무료 경로 FP가 늘 수 있다(추가 후보 5개 중 2개가 가짜였다). 잠정안은 "비전 ON에서만".
- 재귀 깊이 상한 — 무한 분할 방지.
- 7/23에서 **5개는 여전히 회수 안 된다**(7/12). 나머지는 이 방식의 한계인지, `minSpan` 조정으로 더 잡히는지 확인 필요.

## 6. 검증 계획

| 단계 | 방법 | 통과 기준 |
|---|---|---|
| 오프라인 스윕 | `MAX_SPAN`·`MIN_SPAN` 격자 탐색, 82회차 전량 + 홀드아웃 5회차 | 전량 ≥94.4 · 7/23 ≥70 |
| 유닛 | 재귀 분할 함수 순수 로직(경계 여유·깊이 상한·후보 없음) | 신규 테스트 통과 |
| 회귀 | `npm run check` + `npm run check:news` | 그린 |
| 실기 | 7/23 Premiere 실전(비전 ON) | 아이템 ≥7개 · 오배제 0 유지 |

## 7. 천장을 뚫으려면 — 비전 제안 (미검증)

무료 신호가 못 찾는 5개를 사람은 프레임을 보면 즉시 안다(사용자 확인 완료 — 놓친 8샷은 육안으로 정상 앵커샷). 즉 **판별 정보는 픽셀에 있고 luma 격자 표현에 없다.** 남은 현실적 경로는 비전을 **검증자가 아니라 제안자로** 쓰는 것이다.

- 안전망이 발동한 회차(뱅크 일치도 > 0.10)에 한해, 긴 구간을 ~10초 간격으로 훑어 "앵커샷인가"를 묻는다.
- 7/23의 490초 구간이면 약 49프레임(기존 68장 대비 +72%) — 이상 회차에만 붙으므로 정상 회차 비용은 0.
- 미검증 위험: 비전은 **오배제 0**으로 튜닝돼 있어 "제외에 보수적"이다. 제안자로 쓰면 성향이 반대라 프롬프트·임계를 새로 잡아야 한다.

## 8. 후순위로 남기는 것

- **참조 뱅크 성장**(사용자가 확인한 결과를 로컬 참조로 승격) — 근본 해법이지만 오답 학습·뱅크 오염 위험이 있고 승인 UI 설계가 선행돼야 한다. 위 재탐색이 목표를 달성하면 우선순위가 내려간다.
- **새 특징 도입**(인물 위치·고해상 스캔) — 비용이 크고 §100 처방 ④로 별도 관리.

## 9. 참고

- 실측 근거: [HOST_BETA_RUNBOOK.md](../../HOST_BETA_RUNBOOK.md) §100
- 안전망(구현 완료): `bankFitDistance()` · `BANK_FIT_WARN_DISTANCE=0.1` — 커밋 a7d13c1
- 실험 스크립트: scratchpad `gap-split.mjs` · `region-sweep.mjs` · `longitem-risk.mjs`
