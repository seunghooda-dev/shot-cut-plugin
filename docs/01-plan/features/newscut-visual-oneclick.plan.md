# 원클릭 분할 무료화(STT 제거) Plan

## 배경·요구(사용자 지시 2026-07-20)
- "원클릭 분할에서 STT는 빼줘. 내보내기 세부 설정·폴더 설정은 네가 다 해줘 — 나는 원클릭 버튼만 누르면 되게."
- 기존 원클릭 = STT(유료·느림) → 자막 텍스트 분석(유료) → 생성 → 내보내기(사전 설정 필수).
- §57~64에서 검증한 화면(앵커 샷) 기반 분할이 자막 없이 동일 품질을 냈다 — 이를 버튼에 탑재한다.

## 목표
1. 원클릭에서 STT·자막 의존 완전 제거 — 활성 시퀀스 화면 스캔만으로 분할.
2. 내보내기 프리셋·폴더 미설정 시 기본값 자동 적용(YouTube 1080p HD.epr + `C:\Users\seung\Videos\premiere_내보내기`) — 버튼 하나로 렌더 완료까지.
3. 품질 = §62 확정 파이프라인 수준. 오프라인 실험(16회차 회귀 코퍼스) 결론에 따라
   **후보 도출은 무료 그리드 매칭, 최종 앵커 판정은 기존 비전 분류기(`classifyAnchorShots`)** 하이브리드.
   비전 실패(키 없음·쿼터) 시 휴리스틱 전용으로 자동 강등 + 경고.

## 비목표
- 일요일(레터박스) 포맷 완전 자동화 — 참조 매칭 무용(§63). 비전 가용 시에만 커버.
- 기존 자막 기반 세부 단계 버튼(분석/생성/내보내기) 동작 변경 — 그대로 유지.

## 검증
- 오프라인: 16회차 캐시 그리드 회귀(주 앵커 재현·정적 꼬리 16/16 일치 확인 완료).
- 게이트: `npm run check`(신규 순수 로직 단위 테스트 포함).
- 실기: Train_KBC_20260717_Fri에서 원클릭 실행 → 정답 경계(§64)와 비교, 렌더 산출물 확인.
