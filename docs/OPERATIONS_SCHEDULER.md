# 자율 스케줄러 — 병목 무발생·개선 무중단 (2026-08-08 사용자 지시)

> "앞으로 병목이 발생하고 개선되지 않게 자체적으로 스케줄러 짜줘" — 이 문서가 그 스케줄러다.
> 매시 점검 크론은 이 문서의 큐를 평가해 최상위 실행 가능 항목을 기동한다. 세션이 재시작되면
> 이 문서부터 읽고 크론(매시 :23)을 다시 건다. 실사고 근거: 2026-08-08 04~19시, "신규 없음 +
> 사용자 부재 + Premiere 열화"가 겹치자 15시간 동안 점검만 반복되고 개선이 멈췄다.

## 원칙 네 가지

1. **대기 조건이 생기면 그 조건이 필요 없는 작업으로 즉시 전환한다.** "변화 없음" 보고만
   남기는 유휴 사이클은 스케줄러 실패다 — 매 사이클 반드시 하나를 실행한다.
2. **백로그는 항상 3개 이상 유지한다.** 소진되면 "발굴 사이클" 자체가 다음 작업이다.
3. **실기(단일 CDP 세션)와 자원이 겹치지 않는 작업은 병렬로 진행한다** — 실기 체인 중에도
   오프라인 분석·문서·다운로드는 돈다(단 빌드(npm run build/check)는 체인 중 금지 — dist 오염).
4. **서브에이전트는 항상 최대로 고용한다**(2026-08-09 사용자 지시). 실기 세션과 겹치지 않는
   일 — 프레임 판독, 대량 탐색, 코드 감사, 외부 자료 조사, 보고서 작성 — 은 **기본적으로
   에이전트에 위임하고 여러 명을 동시에 돌린다.** 혼자 순차로 처리하는 것이 기본값이 아니다.
   - 매 사이클 "지금 병렬로 돌릴 수 있는 조사·감사·판독이 무엇인가"를 먼저 묻는다.
   - 종류 제한 없다(general-purpose·Explore·code-analyzer·Plan 등 일에 맞는 것).
   - 위임해도 **판정·확정은 본인이 한다** — 라벨 확정은 본인 재검증 필수(§측정 규약),
     에이전트 보고는 1차 자료로만 쓴다.
   - 실기(단일 CDP)와 빌드는 위임 금지 — 세션·dist 충돌.

## 우선순위 큐 (매 사이클 위에서부터 첫 실행 가능 항목을 기동)

| 순위 | 항목 | 실행 가능 조건 |
|---|---|---|
| P0 | 신규 회차 ③ 측정 — §151 오염 검사 → 라벨(에이전트 1차+본인 재검증) → 유료 3실행(§194 신뢰판) → 검문 채점 | 신규 파일 + 패널 정상 + 크레딧 |
| P1 | 예약된 실기 검증(백로그의 [실기] 항목) | 패널 정상 (+유료면 크레딧) |
| P2 | 개선 백로그(아래 표) 중 현재 병목과 무관한 최상위 | 항목별 조건 |
| P3 | 문서·테스트·정합·위생 | 없음 |
| P4 | 발굴 사이클 — 백로그 보충(실측 로그 FP/FN 재분석·코퍼스 확장 탐색·외부 기법 조사) | 없음 |

## 병목 대응 표 (조건 발생 시 전환 규칙)

| 병목 | 전환 |
|---|---|
| 사용자 개입 대기(패널 Load·재기동) | 오프라인·문서·라벨·다운로드 항목으로 전환, 개입 요청은 반복하지 않고 아침 창에 1회 묶음 제안 |
| 크레딧 소진 | 무료 경로 항목만 진행, 충전 감지는 다음 유료 항목 실행 시 실행기 가드가 겸함 |
| 신규 회차 없음 | 코퍼스 심화 — 과거 회차 탐색·다운로드·라벨(홀드아웃 풀 확보) |
| Premiere 열화(42h 실측 ×2)·행 | 오프라인 항목 전환 + 임계 접근 시 사용자 창에 재기동 제안. §192: 재기동은 자동화 가능, Load만 사용자 필요 |
| 실기 체인 실행 중 | 병렬 무해 작업(오프라인 분석·문서·다운로드), 빌드 금지 |

## 정기 실기 유지보수 (2026-08-09 사용자 지시 — 4시간마다)

**"정해진 시간마다 로드 직접 실행하고 프로젝트 재실행"** — 열화·비대를 사후 대응이 아니라
주기 리셋으로 막는다. 크론이 scratchpad `run-maintenance.sh`를 4시간마다 돌린다.

1. **패널 Load 재실행**(`panel-reload.mjs` — connectPanel의 Plugin.load). §192 세션 열화
   (42h에 2회 실측)를 주기적으로 리셋한다.
2. **새 폐기용 프로젝트 로테이션**(`cdt-rotate-project.mjs`) — 시퀀스 누적 예방.
3. **프리뷰 캐시 회수**(`npm run clean:previews -- --apply`).

**실기 체인 중이면 스스로 건너뛴다**(panel.lock 45분 미만) — 회차 중간 개입은 §40-d 단일
세션 원칙을 깨고 그 회차를 무효로 만든다. 다음 주기에 재시도한다.

### 과부하 방지 3규칙 (2026-08-09 사용자 지시 — "중간에 프리미어 다운되지 않게 관리")

배치가 **회차 중간에** 멈추면 그 회차가 통째로 무효가 된다(로테이션은 §40-d 단일 세션 원칙상
회차 경계에서만 가능하므로 중간 구제가 불가능하다). 사후 대응이 아니라 선검문으로 막는다.

1. **배치 시작 전 시퀀스 선검문** — `cdt-mw-paid-batch.mjs`가 접속 직후 활성 프로젝트의 시퀀스
   수를 세고 **200개 초과면 배치를 시작하지 않고 exit 2**로 거절한다(운영자가 로테이션 후
   재기동). 멈춤 실사고 때가 4400시퀀스·58MB였으므로 200은 넉넉한 여유를 둔 선이다.
2. **실기 배치 중에는 무거운 서브에이전트를 새로 띄우지 않는다.** 에이전트의 ffmpeg 프레임
   추출·대량 스캔이 같은 메모리·디스크를 놓고 경쟁한다. 조사 과제는 배치 **전후**에 몰아서
   돌리고, 배치 중에는 문서·소스 편집처럼 자원을 안 먹는 일만 한다. 병렬 고용 원칙(4항)의
   예외는 이것 하나다.
3. **매시 점검에 부하 실측을 포함한다** — Premiere 작업 세트·핸들 수·**가동 시간**·활성 프로젝트
   시퀀스 수. 기준선(2026-08-09 정상 시): **3.4GB · 핸들 8137 · 시퀀스 67**. 핸들 20000 초과나
   시퀀스 200 초과면 다음 회차 경계에서 즉시 로테이션한다.
4. **가동 시간이 72시간을 넘으면 사용자에게 재기동을 보고한다 — 자율로 재기동하지 않는다.**
   §192 세션 열화는 42시간에 2회 실측됐고 2026-08-10 새벽 실측은 **78.1시간**이었다(메모리
   3.89GB·핸들 8248로 수치 자체는 정상). 그런데 앱을 재기동하면 **UDT 등록이 끊겨 사용자
   개입 없이는 실기가 재개되지 않는다** — 열화를 막으려다 더 큰 병목(사용자 대기)을 만든다.
   그래서 자율 조치는 **패널 Load 재실행**까지만 하고, 재기동은 보고 후 사용자가 있을 때 한다.

**§192 정정(2026-08-09 실측)** — "패널 Load는 사용자 클릭이 필요하다"는 반쪽 사실이다.
UDT에 앱 클라이언트가 등록돼 있으면 **스크립트가 Plugin.load를 직접 수행할 수 있다**(오늘
실증). 사용자 개입이 실제로 필요한 경우는 Premiere 앱 자체를 재기동해 UDT 등록이 끊겼을
때뿐이며, 그때는 `udt-app-check-wide.mjs`가 실패로 즉시 드러난다.

## 시간대 규칙

- **자정**: 모닝와이드 신규 회차 확인(8뉴스는 보류 지시 유지).
- **매시(:23)**: 큐 평가 → 기동. 검문 3종(락·잔존 프로세스·Premiere 가동 시간) 동반.
- **4시간**: 직전 실적·다음 계획 수립(보고 전에 다음 작업 먼저).
- **5시간**: 실기 로테이션(체인 경계에서만).
- **아침(사용자 활동 감지 시)**: 개입 필요 항목(재기동·Load·결정 대기)을 1회 묶음 제안.

## 백로그 (2026-08-09 갱신 — 완료 시 취소선, 3개 미만이면 P4 발굴 강제)

**A. 데스크행 거리 게이트 — 수치는 좋으나 구조 위험 확인, 구현 보류(2026-08-09 재측정)**

에이전트 제안(FP 39건 전수 판독 → 최하단 행 거리 AUC 0.656 → 0.923)을 **제품 후보 집합
기준으로 재측정**했다(`mw-desk-gate-ab.mjs`·`mw-desk-gate2-ab.mjs`, 19회차).

| 구성 | 학습 F1 | 홀드아웃 F1 | 최악 회차 하락 |
|---|---|---|---|
| 현행 | 94.3 (FP 20) | 88.3 (FP 19) | — |
| 단일 조건 desk>0.08 | 97.3 (FP 9) | 92.8 (FP 8) | 0.0 |
| 2조건 desk>0.08 & ref>0.05 | 96.5 (FP 13) | 92.8 (FP 8) | 0.0 |

**⚠️ 이 판정의 안전 진단은 무효다 — 재측정 대기(2026-08-09 저녁 정정).** 진단이 데스크행
거리를 **후보 시각 기준 4초 창**에서 쟀는데, 그 후보 시각 자체가 틀려 있었다(§7-ao) — 런
후보가 b-roll 시각에 앵커 거리를 달고 있어 8/4는 라벨 1085.7 대신 **1082 기자실 b-roll**을,
8/3은 531.3 대신 **526 시약 b-roll**을 쟀다(1080p 프레임 육안 확인). 아래 문단의 "합성 구도는
데스크행 거리가 원리적으로 크다"는 **근거가 무효다** — 8/4의 진짜 앵커는 뱅크 거리 0.039로
그 회차 TP 중앙값(0.038) 수준이다. **§7-ao 시정본으로 A/B와 안전 진단을 처음부터 다시 돌리고,
탈락 목록은 후보 시각이 아니라 라벨 시각의 프레임으로 확인한다. 그때까지 채택도 기각도 아니다.**
다만 하드 필터가 아닌 사용처(P5 모델 특징 · 유료 프레임 예산 우선순위)는 오배제 위험이
원리적으로 없어 여전히 우선순위가 높다.

<details><summary>무효화된 종전 판정 (기록 보존)</summary>

단일 조건은 **진짜 라벨 근처 후보를 24~26건 떨어뜨린다.** 2조건으로 완화해도 22건이다.
그중에는 **두 사이클을 들여 회수한 footage 전면 합성 구도**가 그대로 들어 있다(8/4@1080
= 라벨 1085.7 desk 0.110 · 8/3@524 = 라벨 531.3 · 8/3@430 = 라벨 438). **데스크가 프레임
밖인 구도는 데스크행 거리가 원리적으로 크다 — 이 게이트는 그 유형에 체계적으로 적대적이다.**

F1이 오히려 오른 이유는 같은 라벨을 **다른 후보가 중복으로 덮고 있어서**다. 즉 현재의 안전은
후보 중복이라는 우연에 기대고 있고, 중복이 없는 신규 회차에서는 그대로 FN이 된다. §7-ah·§7-al
두 사이클이 정확히 그 유형을 살리려고 뱅크를 편입한 것이므로 **되돌리는 방향이다.**

**판정: 하드 필터로는 기각.** 남은 설계 후보 — ①데스크행을 필터가 아니라 **P5 모델의 특징**
으로(단 147차원 가드 때문에 8뉴스 모델이 조용히 0점 되는 위험, 별도 배선 필요) ②비전 검증의
**우선순위 신호**로만 사용(무료 경로는 그대로, 유료 프레임 예산을 데스크행 먼 후보에 몰아줌)
③합성 구도를 별도 뱅크 계열로 분리해 그 계열 라우팅 회차에서는 게이트를 끄기.

</details>

산출물 `scratchpad/fp-analysis/`·`mw-desk-gate*-ab.mjs`·`mw-desk-gate*-diag.mjs`.

**B. ~~인용 띠 필터 모닝와이드 이식~~ — 실기 검증에서 기각·원복(2026-08-09)**

8뉴스에서 검증된(라벨 315건 오판 0) 2줄 인용 띠 필터를 MW 무료 경로에 연결하고 실기 무료
3회차로 검증했다(크레딧 0). **결과: 이득 0, 손해 실재.**

| 회차 | 기준(무료) | 필터 ON | 판정 |
|---|---|---|---|
| 8/4 (표적 — D형 FP 879.5) | 96.8 | **96.8** (FP 879.5 그대로) | 표적을 못 지움 |
| 7/22 (대조) | 100 | **94.4** — 진짜 앵커 171·824 삭제 | **오배제 발생** |
| 7/28 (대조) | 85.2 | 85.2 | 무영향 |

**⚠️ 종전에 적은 원인은 실측 반증됐다(2026-08-09 저녁 정정).** "MW 블록 6~10초라 프로브가
다음 꼭지로 이탈"이라고 썼으나, 7/22 171의 블록은 **19.2초**·824는 **12.0초**이고 프로브
프레임은 전부 같은 앵커 샷이었다(원본 프레임 판독). `isSameShotGrid` 가드는 정상 작동했고
막을 이탈 자체가 없었다. 라벨 경계 60곳 실측에서도 첫 샷 전환까지 중앙값 ≈11초로, 7초 미만은
13%뿐이다.

**진짜 원인 두 가지** — ①**표 1개짜리 만장일치**: `votes.every(...)`가 표가 1개일 때도
통과시켜, 824는 +2 프레임에 띠가 아예 없어 **단 한 표로 삭제**됐다 ②**판별 기준 불일치**:
MW 헤드라인 띠는 따옴표를 상시 쓰는데(`'장윤기 사건' 형사과장 영장 기각`) §141 기하는 8뉴스
조판 기준이다. **재시도 조건**: `votes.length >= 2` 요구 · 띠 부재 프레임을 "판정 보류"로
명시 · 따옴표를 인용 근거에서 빼고 줄 수·글리프 높이만 쓰도록 MW 전용 임계 재보정.
**짧은 오프셋(+1.2/+2.5)은 방향이 반대라 기각한다** — 아래 D③ 참조.

**C. 라벨 결손 판정(사용자 확인 후보)** — FP로 분류된 C형 3건(7/29@822·7/15@316·7/16@1004)이
실은 라벨 누락일 수 있다(배경·주제가 직전 아이템과 완전히 다름). 프레임 재확인으로 판정.
사실이면 무료 정밀도 상한이 측정치보다 높다.

**D. 감사 잔여(2026-08-09 code-analyzer 12건 중 미시정 — 저녁 재검토로 3건 종결)** —
~~①합집합 cap 0.09가 3계열 기준인데 뱅크는 7계열~~ **종결(§7-ao)**: 7계열·19회차 재스윕에서
최적이 0.08로 되돌아왔고 채택했다(TP·FN 불변, FP만 감소). ②`runYieldMaxDist`가 cap과 같은
상수를 공유 → 상수 분리 — **다만 분리보다 먼저 옵션 존치 여부를 판정한다**(아래 F).
~~③프로브 +4/+7이 짧은 블록에서 블록 밖~~ **기각**: 배제 413건 전수 대조에서 라벨 소실은
2건뿐이고, 7/22 962는 **+7만이 앵커라 유일한 방어표**였다 — 짧은 오프셋으로 바꾸면 그 후보가
4/4 배제돼 §92 오배제 0이 즉시 깨진다. ~~④`mw-offline-gate.mjs`가 리포 밖에만 있어 사라져도
무신호~~ **종결**: `npm run check:mw`로 승격했다(`scripts/morning-wide/regression.mjs`). 스캔
캐시도 스크래치패드에서 `training-data/morning-wide/scan-cache/`(gitignore)로 옮겨 세션이 바뀌어도
살아남는다. 학습 13 + **홀드아웃 6회차**를 함께 채점하며 19회차 전원 통과가 현재 기준선이다.
check:news와 달리 제품 모듈(`.test-build`)을 그대로 불러 쓰므로 뱅크 라우팅·P5·합집합이 제품과
같은 코드다. **모델·뱅크·문턱을 바꾸면 스냅샷도 같은 커밋에서 다시 잰다.**
⑤NaN fail-open(`refDist >= cap` 폴라리티 — 현행 데이터에서 NaN 0/896이라 발동 조건은 없음)
⑥`preferPrimary:false` 무테스트.

**F. ~~[실기] `runYieldMaxDist` 존치 판정~~ — §7-ao가 해소했다(2026-08-09 밤 종결)**

옵션의 실측 손해는 "정확한 컷을 4~6초 이른 컷으로 바꾼다"였는데, **그 기전이 곧 §7-ao가 고친
시각 귀속 결함이었다** — 옵션이 살린 런의 시각이 `run.start`(직전 b-roll)였기 때문이다.
`runTimeAtOnset` 적용 후 실기 4회차에서 원 표적 7/22·7/29 계열과 과거 FP 이력 7/28이 모두
**F1 100 · FP 0**이라 제거할 이유가 없다. 오프라인에 남은 FP 2건 차이는 실기에서 비전이
흡수한다(§163이 8뉴스에서 규명한 것과 같은 중복). **제거하지 않는다.** D②의 상수 분리는
소비자가 둘 그대로이므로 별개 항목으로 남는다.

<details><summary>종전 제안 (기록 보존)</summary>

옵션은 후보 67개를 더하고 그중 30개가 라벨 인접인데, **최종 채택에서 구제한 라벨은 0건**이다
(19회차 포렌식). 원 표적 3지점(7/22 968·7/29 370·7/29 860)은 §7-an P5 v2가 이미 정확히
검출하고, 옵션은 컷을 4~6초 이르게 만드는 쪽으로만 작동했다. 오프라인에서 옵션을 끄면
학습 96.2→96.7 · 7/28 97.9→100 · 7/31 97.3→100이고 **하락 0**이다.
**그러나 회수 기제를 없애는 변경이라 오프라인만으로 판정하지 않는다**(회수 경로 검증 2원칙) —
표적 7/22·7/29 + 과거 FP 최다 7/28을 대조에 넣어 실기로 판정한다. 통과하면 D②의 상수 분리는
소비자가 하나만 남아 자동으로 불필요해진다.

</details>

**G. ~~[실기] 모델 tau 0.65 A/B~~ — 기각(2026-08-10 새벽, 크레딧 0 소모)**

에이전트 수치는 **start 모드·cap 0.08** 기준이었다. §7-ao 시정본(onset·cap 0.08 = 현행 제품
구성)에서 다시 재니 결과가 달라졌다.

| tau | 상승 | 하락 | 회수 라벨 | 신규 FP |
|---|---|---|---|---|
| 0.70 | 3 | 1 | 8/6@947 · 7/15@883 · 7/17@841 | 7/31@820 · 7/17@296 |
| **0.65** | 3 | **3** | +8/6@28·734 · 7/15@1019 · 7/17@683 | +7/22@632 · 7/29@286 |
| 0.60 | 3 | 6 | +8/6@775 | +7/27@860 · 7/20@540 · 7/21@364 · 7/15@756 |

"FP +1뿐"이라는 종전 판단은 재현되지 않는다 — 0.65는 **상승 3·하락 3**이다.

**결정적 기각 근거는 §163의 중복 논리가 MW에서도 성립한다는 것이다.** 완화가 회수하는
라벨은 8/6에 집중돼 있는데(947·734·28), **8/6은 오프라인 77.4인데 실기 100×3이다** — 그
결손을 유료 회수 경로가 이미 전부 메우고 있다. 즉 tau 완화는 이미 가진 재현율을 무료 경로에서
한 번 더 사는 대신 정밀도를 내주는 거래다. **실기 A/B에 크레딧을 쓸 이유가 없어 오프라인에서
닫는다.** `VISUAL_MODEL_TAU`는 8뉴스와 공유하는 상수이므로 손대지 않았다(제품 변경 0).

<details><summary>종전 제안 (기록 보존)</summary>

MW판 재측정에서 tau 0.75→0.65가 홀드아웃 재현율을 95→**100**으로 올리고 FP는 +1뿐이다
(8뉴스 §163의 "잠재 FP 14"와 대조적). 다만 §163의 기각 논리(재현율 이득이 유료 회수와 중복)가
MW에서 미검증이고, 회수 라벨 9건 중 4건이 이미 실기 100인 회차라 중복 가능성이 실재한다.
**`VISUAL_MODEL_TAU`는 8뉴스 공유 상수이므로 절대 상수를 고치지 말고** `detectModelStarts`의
세 번째 인자로 MW 호출부에서만 전달할 것.

</details>

**E. 에지 표현 실험(중장기·8뉴스 동결 주의)** — 같은 픽셀에서 luma 대신 **에지 채널**을 쓰면
교차회차 판별력 AUC 0.738 → 0.970(띠 영역 제외해도 0.918, 현행 96px 스캔 폭 유지 가능).
"판별 정보가 luma 격자에 없다"는 오래된 결론의 해법 후보. 제품 변경 0으로 오프라인 재스캔
→ 뱅크·모델 재구성 → 홀드아웃 판정까지 하고, 통과 시에만 제품화(공용 코드라 check:news 필수).
산출물 `scratchpad/repr-compare.mjs`.

## 구 백로그 (2026-08-08 시드)

1. **[실기·유료] mwCT 대조 체인 채점·판정** — 7/28 FP 8건 비전 소거 실증 + 7/31 §7-ai 옵션
   실기 확인. (진행 중)
2. **[다운로드→라벨→학습] 과거 회차로 홀드아웃 풀 확보** — 7/21 이전 회차의 YouTube 존재
   탐색(dl-aug.sh 계열 재사용) → 다운로드·H.264 → 라벨(§151 검사) → **P5 10회차 재학습
   게이트 재개**(현재 홀드아웃 부재로 보류된 것을 해제). 신규 회차 가뭄과 무관하게 진행 가능.
3. **[실기·무료] 편입 후 무료 하한 미갱신 7회차 재실측** — 7/22·23·24·27·29·30·31
   (§7-ah 부기는 3회차만 갱신). 크레딧 0.
4. **[오프라인] 8뉴스 무료 오프라인 전량 회귀 감시** — 동결 중 회귀 방어(전량 재검증 금지
   예외로 명시된 "무료 오프라인 전량은 무방").
5. **[문서] §192 세션 수명 42h 2차 실측 런북 등재** — 8-08 15:23 열화 재현(2분+ 지연) +
   패널 재로드로 회복된 관찰 포함.

## 이 스케줄러가 바꾸지 않는 것

- 8뉴스 동결(신규 작업 금지·회귀 방어만), 전량 실기 재검증 금지, §194 실행기 규약,
  측정 위생 검문 6항, 커밋·푸시 규약 — 전부 상위 규약 그대로.
