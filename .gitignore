node_modules/
dist/
release/
.test-build/
.ai-test-build/
.thumbnail-test-build/
.job-test-build/
.recovery-test-build/
.final-qc-test-build/
.qa-*-build/
beta-evidence/
speech-evidence/
local-whisper-evidence/
host-smoke-assets/
*.log
.DS_Store
Thumbs.db
# bkit 로컬 상태 (타임스탬프·히스토리 상시 갱신 — 커밋 노이즈 방지)
docs/.bkit-memory.json
docs/.pdca-status.json
docs/.pdca-snapshots/
# 테스트 산출물(TTS 등 로컬 검증에서 생성) — 커밋 대상 아님
tests/*.wav

# STT 출력(사용자 출력 폴더가 저장소를 가리킬 때 생성되는 파일)
tests/*_ShortFlow.srt
tests/*_ShortFlow.txt

# 뉴스 앵커 학습 데이터 — 스캔 캐시(재생성 가능·대용량)는 제외, 사람 검증 라벨(items)만 커밋
training-data/news-anchor/*
!training-data/news-anchor/*.items.json
!training-data/news-anchor/*.items.corrected.json
!training-data/news-anchor/NewsCut_KBC_20260715.json

# 모닝와이드 학습 데이터 — 같은 규칙(스캔 캐시 제외, 라벨만 커밋). 코퍼스·지표는 8뉴스와 분리한다.
training-data/morning-wide/*
!training-data/morning-wide/*.items.json
!training-data/morning-wide/*.items.tile.json
